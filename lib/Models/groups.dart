class Groups {

  String groupName;
  List groupSearchName;
  String groupImage;
  String description;
  int memberCount;
  bool private;  
  String admin;
  List members;
  DateTime createdAt;

  Groups({this.groupName, this.groupSearchName, this.groupImage, this.description, this.memberCount, this.private, this.admin, this.members, this.createdAt});

}